// Dílčí sitemapa sekce. Statická routa (ne dynamická) záměrně — viz
// sectionSitemapRoute v src/lib/sitemap-entries.ts.
import { sectionSitemapRoute } from '../../lib/sitemap-entries';

export const GET = sectionSitemapRoute('de');
